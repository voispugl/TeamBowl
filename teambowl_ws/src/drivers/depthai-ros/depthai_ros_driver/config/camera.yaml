/**:
  ros__parameters:
    camera:
      i_enable_imu: true
      i_enable_ir: true
      i_nn_type: none
      i_pipeline_type: depth
    stereo:
      i_align_depth: true # Required to map depth to RGB
      i_publish_topic: true
      i_decimation: 4
    point_cloud:
      i_num_threads: 4      # Use all Jetson cores for the math
      i_sync_threshold: 100 # Match your previous logic
